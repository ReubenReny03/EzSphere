import { Users } from 'lucide-react';
import { EmptyState } from '@/components/ui/EmptyState';

export const DiversityTab = () => (
  <EmptyState icon={Users} message="Diversity Dashboard is coming in Phase 1" />
);
