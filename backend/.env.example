PORT=5000
MONGO_URI=mongodb://admin:admin123@100.86.230.112:27017/ezsphere?authSource=admin
JWT_SECRET=ezsphere
JWT_EXPIRES=1d
# CORS already auto-allows localhost/LAN/Tailscale origins (see src/config/cors.js);
# only add extra explicit origins here (comma-separated) for anything outside that, e.g. a real domain.
CLIENT_ORIGIN=http://100.67.178.28:5173
ENABLE_SIMULATOR=true
UPLOAD_DIR=uploads
NODE_ENV=development